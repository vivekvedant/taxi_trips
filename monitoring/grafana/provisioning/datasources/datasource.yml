apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: http://prometheus:9090
    basicAuth: false
    isDefault: true
    editable: true
    
  - name: gdev-loki
    type: loki
    access: proxy
    url: http://loki:3100
    editable: false
  # - name: Loki
  #   type: loki
  #   access: proxy
  #   orgId: 2
  #   url: http://loki:3100
  #   basicAuth: false
  #   isDefault: false
  #   editable: true

    # - name: Prometheus
    # type: prometheus
    # access: proxy
    # orgId: 1
    # url: http://prometheus:9090
    # basicAuth: false
    # isDefault: true
    # editable: true